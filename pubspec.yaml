name: prediccion_coches
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^01.3.0
  provider: ^6.1.4
  path_provider: ^2.1.5
  csv: ^6.0.0
  lottie: ^3.3.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
      - assets/coches_data.csv
      - assets/coches_data_cleaned.csv  
      - assets/images/car_logo.png